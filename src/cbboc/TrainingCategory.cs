namespace cbboc
{
    public enum TrainingCategory
    {
        NONE = 0,
        SHORT = 1,
        LONG = 10,

        ///////////////////////////////

        //(Java) private readonly int multiplier;

        //(Java) private TrainingCategory(int multiplier)
        //(Java) {
        //(Java)    this.multiplier = multiplier;
        //(Java) }

        //(Java) public final long getMultiplier()
        //(Java) {
        //(Java)    return multiplier;
        //(Java) }
    }
}
